<div class="user_page">
  <header class="user_page-header">
    <div class="user-name">
      <h2><%= @user.nickname %></h2>
    <div class="navbar">
      <ul>
        <li><%= link_to 'コメントした動画', comments_user_path(@user) %></li>
        <li><%= link_to 'いいねした動画', likes_user_path(@user) %></li>
      </ul>
    </div>
    </div>
  </header>
    <div id="section-contents-inner-video">
      <div class="section-contents-inner-video-inner">
          <ul class="user_page-contents">
          <% @likes.each do |like| %>
            <li>
            <%= render partial: 'web/videos/partials/video_player', locals: {
                  video_url: like.video.file_url,
                  thumb_url: like.video.thumb_url
                }%>
              <div class="meta">
                  <div class="meta-title">
                    <%= like.video.title %>
                    <%= render partial: 'web/videos/partials/like', locals: {video: like.video} %>
                  </div>
              </div>
            </li>
            <% end %>
           </ul>
      </div>
    </div>
</div>
